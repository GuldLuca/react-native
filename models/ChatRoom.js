class ChatRoom {

    constructor(id, imageUrl, title, messages) {
        this.id = id;
        this.imageUrl = imageUrl;
        this.title = title;
        this.messages = messages;
    }
}

export default ChatRoom;